# VoxNexus Worker

The voice agent worker for VoxNexus - handles real-time voice interactions using configurable LLM, STT, and TTS providers.

## Installation

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[openai,livekit]"
```

## Usage

```bash
python main.py
```
